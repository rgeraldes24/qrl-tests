# Beacon API suite

This live suite validates QRL Beacon, Node, and Config REST responses against a
running network. It cross-checks head and state roots, validator counts, common
configuration, operation pools, and every consensus signature carried by a
live block.
