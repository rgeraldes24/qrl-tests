# Resilience suite

Runs native Kurtosis service lifecycle scenarios against a multi-participant
QRL network. It covers execution and consensus outages, Engine reconnection,
pair restart, and catch-up without deploying a proxy or fault-injection
service.

Run with `DEVNET_PROFILE=sync` or `DEVNET_PROFILE=multi`.
