# Consensus suites

Consensus suites cover Qrysm services directly. Beacon API and validator API
coverage shares the `api` package, while protocol, sync, cold-state, and
optimistic-sync behavior remains in focused packages.
