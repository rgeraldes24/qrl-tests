# Consensus suites

Consensus suites cover Qrysm services directly. Beacon API and validator API
coverage belong in separate packages because they use different endpoints and
service lifecycles.

Add `beaconapi` and `validatorapi` packages here when that coverage is added.
